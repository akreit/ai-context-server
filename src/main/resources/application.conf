server {
  host = "localhost"
  port = 8080
}

claude {
  api-key = ${ANTHROPIC_API_KEY}
  model = "claude-haiku-4-5-20251001"
  max-tokens = 4096
}

mcp-servers {
  github {
    command = "npx"
    args = ["-y" "@modelcontextprotocol/server-github"]
  }
}