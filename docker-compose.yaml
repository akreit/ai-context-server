version: '3.8'
services:
  http-server:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: http-server
    ports:
      - "8080:8080"
    environment:
      SERVER_HOSTNAME: "0.0.0.0"
      SERVER_PORT: "8080"
    restart: unless-stopped

